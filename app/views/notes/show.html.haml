.container
  .note-card
    %h1= @note.title
    %p= @note.content


  = link_to "Edit", edit_note_path, class: 'btn btn-default'
  = link_to "Delete", note_path, method: :delete, data: {confirm: "Are you sure?"}, class: 'btn btn-default'