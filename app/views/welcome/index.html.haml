%h1 Welcome#index
%p Find me in app/views/welcome/index.html.haml


%nav
  .container
    - if user_signed_in?
      Signed in as #{current_user.email}. Not you?
      .btn.nice-button= link_to "Sign Out", destroy_user_session_path, method: :delete

    - else
      .btn.nice-button= link_to "Sign Up", new_user_registration_path
      or #{link_to "sign in", new_user_session_path}